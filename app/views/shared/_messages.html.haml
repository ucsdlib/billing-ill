- flash.each do |name, msg|
  %div(class="alert alert-#{name} alert-success")
    %a.close(data-dismiss='alert') &#215;
    = content_tag :div, sanitize(msg), :id => "flash_#{name}" if msg.is_a?(String)
